<%= form_for(@lifestyle_product) do |f| %>
  <% if @lifestyle_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lifestyle_product.errors.count, "error") %> prohibited this lifestyle_product from being saved:</h2>

      <ul>
      <% @lifestyle_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :lifestyle_id %><br>
    <%= f.number_field :lifestyle_id %>
  </div>
  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.number_field :product_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
